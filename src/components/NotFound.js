import { Container } from "react-bootstrap";

const NotFound = () => {
    return(
        <Container>
            <h1>Request Page is not Found...</h1>
        </Container>
    )
}

export default NotFound;