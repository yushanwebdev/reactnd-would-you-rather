import { Container } from "react-bootstrap";

const NotFound = () => {
    return(
        <Container>
            <h1>Requested Page is not Found...</h1>
        </Container>
    )
}

export default NotFound;